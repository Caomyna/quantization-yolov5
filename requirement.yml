name: .venv-quant

channels:
  - conda-forge
  - pytorch

dependencies:
  - python=3.9
  - numpy
  - matplotlib
  - onnx
  - onnxruntime
  - pillow
  - psutil
  - pip
  - pip:
      - ultralytics
      - onnxconverter-common
      - pycocotools